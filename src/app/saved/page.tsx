export default function SavedPage() {
  return (
    <div className="flex flex-col items-center justify-center h-full space-y-4">
      <h1 className="text-3xl font-bold">Saved Niches</h1>
      <p className="text-muted-foreground">This feature requires additional database modeling (Phase 2). For now, use the dashboard to research niches dynamically.</p>
    </div>
  );
}
